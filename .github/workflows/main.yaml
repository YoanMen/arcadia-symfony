name: CD
on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest

    env: 
      SSHPASS: ${{ secrets.APP_SSHPASS}}
    steps:
      - uses: actions/checkout@v4

      - name: connect to server
        run: |
          ssh ${{ secrets.APP_SSH}}
